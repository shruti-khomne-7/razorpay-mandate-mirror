# JSON object mapping each principal to a unique API key (at least 16 characters).
MANDATE_API_KEYS={"principal_alice":"replace-with-a-long-random-demo-key"}
MANDATE_SECRET_KEY=replace-with-a-long-random-server-only-signing-key
MONGODB_URI=mongodb://127.0.0.1:27017/mandate_mirror
PORT=5000
